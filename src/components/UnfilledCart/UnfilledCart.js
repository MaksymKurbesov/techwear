import React from "react";
import styles from "./UnfilledCart.module.css";

const UnfilledCart = () => {
  return <p className={styles.unfilledCart}>Нет товаров в корзине</p>;
};

export default UnfilledCart;
